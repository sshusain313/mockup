'use client'

import React, { createContext, useContext, useState, ReactNode } from 'react';
import { Mockup, MockupPlaceholder, sampleMockups } from '@/lib/mockup-utils';

// ... existing code ...

export { MockupContext };